# .env

# Your Brave/Chrome extension’s ID (find via chrome://extensions)
CHROME_EXT_ID=gkjjffjldeheenhcnkmkadfmgbnbmpgi

# Path (relative to app.py) to the model you want to load
PICKLE_MODEL_PATH=./models/trainedmodel.pkl

# Detection threshold (probability above which we flag “PHISHING”)
PH_THRESHOLD=0.8

# Where to store the JSON log of alerts
ALERTS_PATH=alerts.json

# Port the Flask app will listen on
PORT=5030
